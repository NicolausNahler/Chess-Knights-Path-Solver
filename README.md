# Chess-Knights-Path-Solver
A python application which finds the fastest path a knight can take from the starting position to the target position. The output is an image of the board created by using PIL.
